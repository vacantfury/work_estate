<!-- skeleton template · genome: corpus/portable-skills.md (project-founding step 5) · remove this line at instantiation -->
# store — instructions

**Scope:** typed data + scoring service; registers are its views; the data-shape rule is its contract.

Genome modules (canonical law — CITED, never copied): `store`. Estate-wide law lives in the hub's instructions layer; this file states only this project's own scope and pointers. Task surfaces per `corpus/task-convention.md`; state crosses this project's boundary only via the crossing protocol (`corpus/estate-structure.md`).
