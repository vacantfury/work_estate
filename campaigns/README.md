# Campaign catalog

The SOLE campaign catalog of this repo (task standard §27.5): every campaign on disk is
listed here; automation enumerates from this file, never by tree-crawl. Seeded externally
`automation/campaign_catalog_audit.py --write`; sessions keep it current at every campaign
touch and annotate lines (state, owner, notes) freely.

- `skeleton/hub/_forms`
