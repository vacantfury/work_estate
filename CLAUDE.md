# CLAUDE.md

**Visibility: public** (flipped from private 2026-08-06, after a clean full-history scan): public so the SEED channel is credential-free — clone and pull need no authentication on the work device. (The original stronger claim — no personal credential ever touches the corp machine — was superseded 2026-08-15 by the graded-boundary ruling, design principle 17: the user's own credentials may land in revocable per-device, footprint-registered form; see `corpus/boundary-protocol.md` credential rule.) Committed content was held to **public-grade discipline from birth** by hard rule: the clone target is an employer-managed machine, so everything committed is written as employer-readable — no personal data, no secrets or secret-manager references, no private-system internals. Founded 2026-08-03, entirely before the maintainer's employment start date: all content is the author's own work, authored on personal equipment with clean provenance.

## What this repo is

An **open work tool**, shaped as an installation package: a portable working-conventions toolkit for operating well on an employer-managed work device. The committed repo stays permanently generic; it is cloned onto a work device, where its unfold procedure detects the environment (work docs + device) and FOUNDS the work-side ESTATE — many projects (hub · people · company intelligence · records · health · communication · risk · resources · performance · research satellite · per-work-project homes), the same estate structure the user works in anywhere, restricted to work-relevant parts and extended with work-native ones (design principles 19–20) — all machine-local, never pushed back. It contains ONLY:

- the corpus: portable working discipline a work environment may use — task convention, session working style, engineering standards, generically useful skills;
- idea/direction notes about how to structure work-machine workflows;
- `UNFOLD.md`, the installer procedure.

**On a work machine, the first session's duty is to run `UNFOLD.md`.** Full model: `text_docs/design.md`.

## Scope boundary (load-bearing)

**This repo owns:** portable working discipline safe for a corporate device — e.g. the ordered-TODO task convention, engineering standards (uv, parameters-in-config-not-code), session discipline.

**Never enters this repo:** personal data of any user's, anything derived from any employer, and knowledge about how any particular user relates this tool to the rest of their life — the tool describes only itself.

**The division, one line (settled 2026-08-06):** this installation = the job (day-to-day operations inside the employer boundary) · the employment's wider meaning for the user's life is their own affair beyond it. The installation is self-contained; boundary discipline (`corpus/boundary-protocol.md`) binds: employer-confidential content stays in employer systems, and every record the user keeps is written at meaning grain — employer-safe by construction.

## Hard content rules

1. **Employer-readable assumption:** anything committed may be read by the employer once the repo sits on a corp device. Write accordingly.
2. **One-way flow, generic in, nothing employer-derived out:** committed content is employer-generic convention only. Anything derived from employer docs, systems, code, or confidential context is NEVER committed or pushed here — it stays machine-local on the work device (`local/`, gitignored). This mirrors the standing IP rule in both directions: nothing from work leaves the employer; nothing of the user's private content enters this repo.
3. **No secrets of any kind** — no values, no secret-manager item names, no tokens, no placeholders that reveal private infrastructure.
4. **Not a bridge:** this repo is not a sync channel; work state never leaves the work device through it.
5. **Open-tool voice and content scope (design principle 18):** committed content speaks as an open, user-generic work tool — "the user" (or "the maintainer"/"the author" in provenance contexts), never a named owner or a personal-ecosystem counterpart — and describes only the tool ITSELF: knowledge about how any particular user relates the tool to the rest of their estate (what they keep out of it, inventories of what stays elsewhere) is that user's own knowledge and never enters the seed.

## Layout

- `CLAUDE.md` — this file: identity + hard rules, loaded by upstream build sessions and by work-side sessions if the sanctioned tooling reads it.
- `UNFOLD.md` — the installer: detect (work docs + device) → resolve the adaptation ledger → generate the adapted form into `local/`. Tooling-agnostic plain markdown — followable by any sanctioned agent, or manually.
- `corpus/` — the genome: the generic convention modules and portable skills the unfold selects from.
- `CHANGELOG.md` — the update manifest: one entry per seed version (SemVer git tags), naming the changed modules and the derived layers they affect; read by `UNFOLD.md` §Update against the installed version.
- `text_docs/design.md` — design record: the installation-package model, settled principles, the adaptation-points ledger. `text_docs/` also holds idea/direction notes.
- `knowledge/` — reference research digests (committed; same employer-readable discipline).
- `local/` — gitignored; holds the unfold's ledger + log, and the degraded-fallback state form. The unfold's real output is the founded ESTATE at the root A4 chooses (outside this clone). Nothing work-machine-specific is ever committed.
- `TODO.md` — gitignored, per the task convention (`corpus/task-convention.md`); tracked in the maintainer's task system off this machine. On the work machine, the estate hub's task head takes this role — the clone itself never carries work state post-install.

## Build state (dated passes; full detail in `text_docs/design.md` and git history)

- 2026-08-03 · v0 seed: corpus modules + `UNFOLD.md`; installation-package model settled.
- 2026-08-04 · axis-2 pass: four-axis build model; `knowledge/` role/workflow research digest + week-1 intake checklist; `corpus/role-playbook.md`; three eval-discipline portable skills.
- 2026-08-06 · operating-posture pass (principle 7) and boundary pass (principle 8; `corpus/boundary-protocol.md`); intake §9.
- 2026-08-09 · time-and-presence pass: principle 9; intake §10; playbook presence convention.
- 2026-08-10 · team-reality pass: principle 10; intake §11; engineering-standards team-reality-first; playbook org additions.
- 2026-08-12 · project-lifecycle pass (principle 11; `corpus/project-lifecycle.md`; intake §12; unfold seeds `local/state/projects.md`) and company-watch pass (principle 12; `corpus/company-watch.md`; intake §13; unfold seeds `local/state/company.md`).
- 2026-08-13 · health-guard pass: principle 13; `corpus/health-guard.md`; intake §14; unfold seeds the tracker.
- 2026-08-14 · work-record pass: principle 14; `corpus/work-record.md`; intake §15; unfold seeds the record.
- 2026-08-15 · dev-workflow pass (principle 15; `corpus/dev-workflow.md`; intake §16), work-engine pass (principle 16; `corpus/work-engine.md` + the research/connections/communication-craft lanes; intake §17; unfold seeds the job registry + lane state), and graded-boundary + research-substrate pass (principle 17; `corpus/research-infrastructure.md` + `corpus/device-return.md`; intake §18; unfold seeds the footprint register).
- 2026-08-17 · record-discipline pass: `corpus/boundary-protocol.md` reconceived as boundary discipline (record grain rule, division of subject matter); `corpus/work-report.md` reframed as the role's own steering loop; weekly-debrief format homed in `corpus/work-record.md`; `corpus/sidework.md` founded.
- 2026-08-19 · axis-1 refresh, final pre-use pass: five law-sync additions (secrets store discovery, instruction-completeness + twice-corrected stop rule, plain-English records, side-effect account registration surfaced before the flow runs).
- 2026-08-19 · open-tool voice pass: principle 18 — the whole committed repo rephrased as an open, user-generic work tool.
- 2026-08-19 · estate-form correction (maintainer): principle 19 — the unfold FOUNDS an estate of many projects (hub + function projects + per-work-project homes), never one super-repo; `UNFOLD.md` Phase 3 rewritten as estate founding; the single-clone `local/` form demoted to the degraded fallback.
- 2026-08-19 · content-scope correction (maintainer): principle 18 extended — the seed describes only ITSELF; user-estate knowledge about the tool (inventories of what stays out, counterpart relations) removed from the purpose, principles 1/2/17, the boundary hard core, and the scope boundary.
- 2026-08-19 · estate-maturation pass (maintainer): principle 20 — roster grown to the full self-management shape: `corpus/benefits.md` (everything the role is eligible for, acquire-at-eligibility, window due lane) + `corpus/career.md` (review readiness + standing promotion case) + the research-satellite roster row (`corpus/research-infrastructure.md` satellite registry); `corpus/people.md` upgraded to relationship-system grade (cards, threads + due lane, campaigns); presence tracking (`site:` line + debrief tally, `corpus/work-record.md`); `corpus/estate-structure.md` (attention view, campaign form, lessons/handbook, failures log + self-audit, data-shape rule); intake §19–§20; UNFOLD Phase 3 steps 9–11.
- 2026-08-20 · hub steward-mind pass (maintainer): principle 22 — `corpus/deliberation.md` (the hub's council: three seats as disjoint groundings — first-person / positioning / delivery — evidence-or-silence, floors bind first, advisory-only, decision records with the deciding project + hub decision log with outcome calibration) + `corpus/self-model.md` (the work-grain profile: identity, targets, working style, calibrations, the user's standing rules; unfold-seeded, debrief/retro harvest loop); hub roster row restated as steward-mind; name `hub` examined and kept.
- 2026-08-20 · translation-completeness pass (maintainer): principle 23 — the upstream working estate walked function by function against the roster, every absence now a NAMED decision; three functions found dropped without one, landed: `corpus/messages.md` (message/meeting flow: triage lanes, drafts prepared never sent, threads on the people cards, meetings entered prepared and left recorded — on the employer's own surfaces) + the placement map and forecast lines (`corpus/estate-structure.md`); intake §21; UNFOLD step 7 extended.
- 2026-08-24 · update-mode pass (maintainer): principle 24 — versioned delta updates: SemVer tags + `CHANGELOG.md` manifest; `UNFOLD.md` §Update (check → delta apply → advance the `HEAD` marker); an installed estate never re-unfolds for a seed change. First tag `v1.0.0`. The first install ran at onboarding (reported 2026-08-24).
- 2026-08-24 · v1.1.0: portable-skills `generalize-at-settle` (the third settle-time transform: scope promotion + parallel sweep; work-side ladder tops at the hub, corpus candidates travel upstream only) + the write-phase line in the improvement-loop dispatch note — closes the last improvement-loop porting gap (absence had no named decision, a principle-23 defect).
- 2026-08-24 · v1.2.0: events join the resources scope as the fifth kind (attendance reads per the data-shape rule: learning · visibility · people vs time cost, attend/skip recommendation, the user decides); not-ported line names the shared scoring/discovery-service absence (the data-shape rule carries the discipline in place).
- 2026-08-24 · v1.3.0: the seed clone becomes a registered, versioned dependency in the hub's estate registry (kind: seed — installed version, channel state, last checked/applied); the update mode's check and apply record onto that row, pending updates surface through the hub attention view; optional A2-gated check job.
- 2026-08-24 · v2.0.0 clear-structure restructure (user decisions): principle 25 — infrastructure pair store (`corpus/store.md`, typed data + scoring service) + search (`corpus/finder.md`, the retrieval bus over the job's channels) founded at end-state (supersedes the scoring/discovery-service absence); communication project dissolved (flow = convention + engine jobs, craft lane under the hub engine, people = the one person container); renames records → worklog, company intelligence → company, research satellite → research; UNFOLD A7/A8 + restructured Phase 3.
- 2026-08-24 · v2.1.0 mimic-check pass (full upstream walk): portable-skills `discovery-dig` (+ digs ride the bus); estate-structure §Kinds and the dependency rule; work-engine cycle pattern (batched attention, results reallocate budget); named absences aid-portfolio + urgency runbooks; README store/search rows backfilled.
- 2026-08-24 · v3.0.0 the clear structure (principle 26, user review sittings): content-kind roster (store/finder/messages trio; people incl. threads; benefits vs career split; main-work manager project; company watch = job not project); store's four scoring folds (Bayesian estimate · balance · forecast · bands); registry EDGES = structure graph + crossing protocol; audits extended to the new surfaces; ample-budget posture.
- 2026-08-24 · v2.2.0 naming pass (user rule: short, or long only for accuracy): generalize · dig · day-start · results-triage · error-analysis · judge-calibration · review-simplify; UNFOLD §Update; naming rule added to session-discipline.
- 2026-08-24 · v3.1.0 infrastructure quartet (user decisions): `corpus/llm_utils.md` (LLM provider seam) + `corpus/devices.md` (compute and device client) + `corpus/autoflow.md` (automation platform; cycle pattern + runtime adaptation move here) + `corpus/auto_research.md` (autonomous research engine; standing jobs move here, candidate funnel added — user + manager are the gate) founded as independent work-stack infrastructure charters at end-state, names kept exact; incumbents behind the seams, per-charter founding gates; estate-wide backup-verified line at the self-audit; kinds roster + cross-refs updated.
- 2026-08-24 · v3.3.0 law-sync pass (second; baseline = the 2026-08-19 axis-1 refresh): every working rule settled or amended since the baseline ported in generic voice or skipped with a named reason — session-discipline (plans-as-ordered-steps, no self-postponement, tool-path order, dissatisfaction-as-defect-report, standing-order persistence, form-stamp + three-lookups founding), task-convention (big-first ranking doctrine, cite-by-position + store-flip view rule), engineering-standards (categorical residual twin, end-state sizing), communication-craft (plain separate sentences, no dash-line connectors), messages (polite-and-concise register, recipient-attention cost), boundary-protocol (user-only `ai_agent_proof` zones), estate-structure (the law layer: record-per-rule + generated view at scale), portable-skills (audit-overlap joins the audit family; terminal-recovery kill-proposal drift fixed), llm_utils (sanctioned-account default vs API as config choice). Named skips recorded in the changelog entry.
- 2026-08-24 · v3.4.0 task-store pass (user decision): the store-backed task form promoted to the STANDARD form — `task-convention.md` §The store-backed form (implementation-grade contract: store truth, generated views with stable ids, append-only ledger, verb write surface, ingest-absorbed hand edits, wake job, flip-once); store consumers + UNFOLD task-head bullet aligned; shipped as spec, not code (named decision — the sanctioned agent builds against the A7 engine).

Next action is at onboarding: run the unfold on the work device — see `TODO.md`.
